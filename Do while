#include <iostream>
using namespace std;

int main() {
    int i = 1; // Initialize the counter

    do {
        cout << i << " ";
        i++; // Increment the counter so the loop eventually ends
    } while (i <= 5);

    return 0;
}
